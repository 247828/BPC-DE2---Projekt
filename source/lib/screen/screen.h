/**
    * Header file for custom library for showing measurements to 16x2 LCD display (for BPC-DE2 project)
    * @author Jakub Kovac
    * 27. 11. 2024
*/

// Functions declaration
extern void lcdInit(void);
extern void updateAngle(float angle);
extern void updateHeight(float height);
//extern void updateAngleBar(float angle);